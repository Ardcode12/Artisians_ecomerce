# Artisans Marketplace Backend Requirements
# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
psycopg[binary]>=3.2.0
pydantic>=2.0.0

# AI / Image Studio
# CPU installation is sufficient; the image service falls back when CUDA is unavailable.
rembg
pillow>=10.0.0
opencv-python-headless>=4.8.0
# The active FastAPI image studio uses rembg, Pillow, and OpenCV directly.
# Real-ESRGAN/BasicSR are legacy dependencies and BasicSR 1.4.2 does not
# build on Python 3.13.

# AI / Multilingual Description & Voice
# Speech-to-text is handled by the remote NVIDIA A100 Whisper Large V3 server.
# faster-whisper is no longer loaded locally; only the requests library is needed.
requests>=2.31.0
openai>=1.0.0
anthropic>=0.18.0

# AI / Pricing Assistant
pandas>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Reel Generation & Social Reach
imageio-ffmpeg>=0.5.0
httpx>=0.24.0

